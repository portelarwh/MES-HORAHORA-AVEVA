{
  "name": "mes-horahora-aveva",
  "version": "4.0.0",
  "description": "Análise de produção a partir do contador do AVEVA Historian: janela exata por data e horário, OEE por base, fechamento por turno, separação entre parada e ausência de dados, qualidade dos dados e relatórios executivos.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node tools/build.mjs",
    "check": "node tools/check.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "verify": "npm run check && npm test && npm run build",
    "dev": "npx --yes serve src -l 5173"
  },
  "keywords": [
    "oee",
    "aveva",
    "historian",
    "mes",
    "lean",
    "manufatura"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
